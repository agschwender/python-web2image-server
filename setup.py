#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='web2image server',
      version='0.1',
      description='Webserver for creating a thumbnail preview of a url',
      packages=find_packages(),
      install_requires=[]
      )
